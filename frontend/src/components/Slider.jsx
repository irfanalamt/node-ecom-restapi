import { Container } from "@mui/material";

const Slider = () => {
  return (
    <Container
      sx={{ width: "100%", height: "100vh", backgroundColor: "blue" }}
    ></Container>
  );
};

export default Slider;
